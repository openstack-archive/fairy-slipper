id:
  in: body
  required: true
  type: string
  description: |
    The snapshot ID.

volume_id:
  in: body
  required: true
  type: string
  description: |
    The source volume ID.

new_file:
  in: body
  required: true
  type: string
  description: |
    The name of the qcow2 file that Block Storage
    creates, which becomes the active image for the VM.

volume_id:
  in: body
  required: true
  type: string
  description: |
    The source volume ID.

snapshot:
  in: body
  required: true
  type: object
  description: |
    The ``snapshot`` object.

snapshot_id:
  in: body
  required: true
  type: string
  description: |
    The snapshot ID.

create_info:
  in: body
  required: true
  type: object
  description: |
    Information for snapshot creation.

type:
  in: body
  required: true
  type: string
  description: |
    The snapshot type. A valid value is ``qcow2``.

tenant_id:
  in: path
  required: false
  type: string
  description: |
    The UUID of the tenant in a multi-tenancy cloud.

